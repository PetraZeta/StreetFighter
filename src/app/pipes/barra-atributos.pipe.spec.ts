import { BarraAtributosPipe } from './barra-atributos.pipe';

describe('BarraAtributosPipe', () => {
  it('create an instance', () => {
    const pipe = new BarraAtributosPipe();
    expect(pipe).toBeTruthy();
  });
});
